## Optimistic rendering in React

- [ ] What is a recommended patterns for optimistic UI (immediately update the UI, before server response comes back, and adjust UI accordingly if it failed)?
  - Do we want to do it? Or display a loader until response?
  - What is the best pattern do do this? Use Apollo's optimistic rendering?
